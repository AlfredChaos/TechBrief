# PRD：TechBrief——OpenAI / Anthropic 官方内容中文博客（Web）

项目名：TechBrief  
版本：v0.2  
状态：草案（待评审）  
负责人（PM）：SOLO（代）  
目标用户：AI 中文读者、站点运营者  
更新时间：2026-06-03  

---

## 1. 背景与问题陈述

OpenAI 与 Anthropic 的公开技术内容更新频繁，形态不仅包括博客文章，也包括视频、演讲、发布会回放等资料。对于中文读者而言，核心问题不再只是“看不懂英文”，还包括：

1) **内容分散**：文章与视频散落在官方博客、官网页面、YouTube 等公开渠道；  
2) **获取成本高**：用户需要自己跟踪多个来源，容易错过重要更新；  
3) **阅读门槛高**：英文长文、技术术语、视频字幕和上下文都增加理解成本；  
4) **运营效率低**：运营者需要反复做“发现、抽取、翻译、发布、通知”这类重复工作；  
5) **缺少统一阵地**：没有一个面向中文读者的统一 Web 入口，用于持续沉淀和分发这些内容。

因此，TechBrief 的新产品形态定义为：

- 一个面向读者的 **公开 Web 中文博客**；
- 一个支持 **邮箱订阅** 的内容分发系统；
- 一个面向运营者的 **后台管理系统**；
- 一个将“发布到微信公众号草稿”的能力下沉到后台中的 **手动发布工具**，而不是主流程的默认出口。

---

## 2. 产品目标（Goals）

### 2.1 业务目标
- 建立一个持续更新的中文内容站点，沉淀 OpenAI 与 Anthropic 的公开文章与视频资料。
- 降低运营者处理公开资料的人工成本，把精力从重复搬运转移到选题、校对和发布策略。
- 形成站内阅读与邮件订阅两条分发通道，提升内容触达效率与复访率。

### 2.2 用户目标
- 读者可以在一个中文 Web 站点中查看最新文章、视频资料及其翻译内容。
- 读者可以通过邮箱订阅，在内容发布后收到更新通知或日报/周报。
- 运营者可以在后台查看订阅者、内容列表、译文状态、每日工作流执行情况，并对内容进行人工发布。

### 2.3 成功指标（MVP）
- 发现新内容到站内可发布草稿生成成功率 >= 95%（以可访问页面或可获取字幕为前提）。
- 发现时延：新内容发布后，系统在 2 小时内完成一次巡检并入库。
- 单次处理耗时：从抓取到生成可审核的中文内容，P50 <= 30 分钟，P95 <= 2 小时。
- 结构保真：标题层级、列表、引用、代码块、关键链接不丢失，抽样验收通过率 >= 98%。
- 订阅链路可用性：订阅、退订、邮件投递流程成功率 >= 98%。

---

## 3. 非目标（Non-goals）

- 不做开放社区功能，如评论、点赞、用户投稿、站内社交关系。
- 不做全自动公众号发布；微信公众号仅作为后台中的人工操作能力。
- 不做多语言输出，MVP 只面向中文展示。
- 不做深度二创内容生产，MVP 以“中文翻译 + 必要摘要/导语” 为主。
- 不承诺解决版权与转载合规问题，最终发布责任仍由运营者承担。

---

## 4. 用户画像与使用场景

### 4.1 用户画像

#### 读者
- 希望快速了解 OpenAI 与 Anthropic 的最新公开动态。
- 更偏好中文阅读，希望保留原文链接、来源和技术上下文。
- 愿意通过邮箱持续接收更新。

#### 运营者 / 管理员
- 负责 TechBrief 的内容审核、发布和订阅用户运营。
- 需要在一个后台中查看内容处理进展、失败原因和每日运行状态。
- 偶尔需要手动录入或发布其他外部文章，并将内容手动推送到微信公众号草稿。

### 4.2 核心场景
1) 用户访问站点首页，浏览最新中文文章与视频内容。  
2) 用户输入邮箱完成订阅，后续收到新内容更新邮件。  
3) OpenAI / Anthropic 发布新文章或视频后，系统自动发现、抓取、抽取、翻译，生成后台待审核内容。  
4) 运营者在后台查看文章及译文列表，确认内容后手动发布到 Web 站点。  
5) 运营者在后台查看每日工作流执行情况，定位失败任务并重试。  
6) 运营者在后台手动录入其他文章，支持自动抓取 URL 或纯手工录入，并将已发布内容推送为微信公众号草稿。  

---

## 5. 产品形态与信息架构

### 5.1 公开站点（Web）
- 首页：最新内容流、来源标签、文章/视频类型标识。
- 列表页：支持按来源、内容类型、发布时间筛选。
- 详情页：展示中文译文、原文信息、原文链接、作者、发布时间、免责声明。
- 订阅入口：支持邮箱订阅与退订。

### 5.2 后台管理
- 仪表盘：每日抓取量、成功率、失败数、待审核数、订阅新增。
- 内容管理：文章/视频列表、译文状态、人工发布、重试、手动录入。
- 订阅管理：订阅者列表、状态、订阅时间、退订状态。
- 工作流管理：每日执行记录、失败原因、阶段耗时、手动重跑。
- 渠道发布：手动创建微信公众号草稿，支持选择已发布内容或手动输入内容。

---

## 6. 需求范围与优先级

### 6.1 P0（MVP 必须）
1) 内容发现：支持 OpenAI 与 Anthropic 的公开文章，以及来自更多公开视频平台的相关视频资料发现，RSS 优先；无 RSS 时轮询列表页。  
2) 去重：按 canonical_url / source_id / 视频唯一链接去重，避免重复入库。  
3) 抓取与落库：保存原始 HTML、视频元信息、字幕文本；无字幕时提取音频并转录为文字稿，用于追溯与重跑。  
4) 正文 / 字幕抽取：结构化提取文章正文；对视频资料提取标题、简介、字幕或转录文字稿。  
5) 中文翻译与研究：保留代码块、命令行、链接结构；维护术语表；输出可审核中文内容；对无字幕视频的转录文字稿额外生成深度研究报告。  
6) Web 内容生成：生成适用于公开博客的文章详情页数据与渲染内容。  
7) 公开站点：支持首页、列表页、详情页基础浏览。  
8) 邮箱订阅：支持订阅、退订、订阅状态记录，并在新内容发布后按每日汇总策略发送订阅邮件。  
9) 后台管理：支持查看订阅者列表、内容及译文列表、每日工作流执行情况。  
10) 人工发布：后台支持将内容从待审核状态手动发布到 Web 站点。  
11) 手动内容录入：后台同时支持录入 URL 自动抓取，以及纯手工录入内容，进入同样的审核发布流程。  
12) 微信公众号草稿：迁移为后台中的手动发布页功能，不再是自动流水线默认输出，并默认复用站内内容封面、摘要和作者信息。  
13) 状态与可观测：每条内容记录处理状态、耗时、失败原因，并支持手动重试。  
14) 文末固定追加：来源、作者、原文链接、发布时间、免责声明模板。  

### 6.2 P1（增强）
- 邮件分发策略增强：周报 / 标签订阅 / 个性化订阅。
- 站内搜索、来源筛选、专题页、SEO 优化。
- 视频转录质量增强：ASR 优化、说话人分离、术语纠错。
- 内容质量检查增强：结构差异检测、链接检查、图片缺失检查。
- 人工审核工作台：中英对照、差异高亮、批注。
- 微信公众号发布历史、失败重试与素材复用。

### 6.3 P2（后续）
- 扩展更多来源站点和频道。
- 自动摘要、要点提炼、译者注、关联阅读推荐。
- 多用户后台、角色权限、运营协同。
- 读者个性化邮件推荐和内容偏好订阅。

---

## 7. 端到端流程（MVP）

### 7.1 自动内容处理主流程
1) Scheduler 触发巡检（每 2 小时一次）。  
2) Discover：抓取 OpenAI / Anthropic 的文章列表，以及更多公开视频平台中的相关视频列表或 RSS。  
3) Fetch：获取详情页 HTML、视频元信息、字幕；无字幕时提取音频并生成转录任务。  
4) Extract：抽取结构化正文 / 字幕 / 转录稿 / 元信息。  
5) TranslateAndResearch：按块翻译，生成中文结构化内容；对无字幕视频转录稿生成深度研究报告。  
6) Review Draft：生成后台待审核内容，状态置为 `review_pending`。  
7) Publish Web：运营者在后台确认后，手动发布到公开站点，状态置为 `published`。  
8) Notify：对已发布内容触发邮件通知任务。  

### 7.2 后台手动操作流程
- 手动录入其他文章：管理员录入 URL 自动抓取，或直接粘贴原文内容纯手工录入 -> 抽取 / 翻译 / 审核 / 发布。
- 手动创建公众号草稿：管理员在后台选择已发布内容或手工内容 -> 默认复用站内封面、摘要、作者信息 -> 生成公众号 HTML -> 调用草稿接口 -> 返回 `wechat_draft_id`。

### 7.3 异常与降级策略
- RSS 不可用：降级为列表页轮询。
- 视频无字幕：后台自动提取音频并转录为文字稿，再基于转录稿生成深度研究报告；若转录失败则进入失败队列等待人工处理。
- 抽取失败：保存原始内容并记录失败原因，允许人工重试或修正。
- 翻译失败：支持指数退避重试，并保留中间产物。
- 邮件发送失败：记录邮件任务状态并支持重发，不影响内容已发布状态。
- 公众号接口失败：仅影响后台手动发布页，不阻断主站内容发布主流程。

---

## 8. 功能需求（详细）

### 8.1 来源管理
**FR-001** 支持来源：OpenAI、Anthropic，以及相关公开视频平台中的官方 / 指定频道内容。  
**FR-002** 支持内容类型：article、video。  
**FR-003** 每个来源支持启用 / 停用。  
**FR-004** MVP 固定巡检频率为每 2 小时一次。  
**FR-005** 记录每次巡检结果、新增内容数、失败数。  

### 8.2 内容发现与去重
**FR-010** 新内容判定优先级：canonical_url > 最终 URL > 平台原始唯一标识。  
**FR-011** `dedupe_key = sha256(source + content_type + canonical_url_or_source_id)`。  
**FR-012** 已存在内容不重复入队；后台允许强制重跑。  

### 8.3 抓取与抽取
**FR-020** 抓取支持条件请求（ETag / Last-Modified）。  
**FR-021** 保存原始 HTML、视频元数据、字幕文本；无字幕时保存音频提取结果与转录文字稿。  
**FR-022** 文章抽取结构单元：H1-H4、P、UL/OL、BLOCKQUOTE、CODE_BLOCK、IMAGE、HR。  
**FR-023** 视频资料至少提取：标题、来源链接、发布时间、简介、字幕 / 转录文字稿 / 文本来源状态。  
**FR-024** 元信息解析：title、author / speaker、published_at、cover_image、source_name。  
**FR-025** 无字幕视频必须触发音频提取与自动转录流程。  
**FR-026** 基于无字幕视频转录稿输出深度研究报告，作为后台可审核产物之一。  

### 8.4 翻译与术语一致
**FR-030** 代码块和 inline code 原样保留。  
**FR-031** 链接 URL 不翻译，链接文本可翻译。  
**FR-032** 标题层级、段落结构、列表关系尽量保持一致。  
**FR-033** 支持 glossary（YAML / JSON）术语表，并执行一致性替换。  
**FR-034** 最小质量检查：标题数量与层级、代码块数量、关键链接数量一致性。  

### 8.5 Web 站点展示
**FR-040** 提供公开首页，展示最新内容。  
**FR-041** 提供内容列表页，支持按来源、类型、发布时间筛选。  
**FR-042** 提供内容详情页，展示中文内容、原文来源、作者 / 讲者、发布时间、原文链接、免责声明。  
**FR-043** 页面需支持文章和视频两类内容的统一展示框架。  
**FR-044** 内容状态至少包含：`discovered`、`processing`、`review_pending`、`published`、`failed`、`archived`。  

### 8.6 邮箱订阅
**FR-050** 提供邮箱订阅入口。  
**FR-051** 订阅成功后记录邮箱、状态、订阅时间、来源页面。  
**FR-052** 每封邮件必须带退订入口。  
**FR-053** 新内容发布后，采用每日汇总策略发送订阅邮件。  
**FR-054** 邮件发送结果需要可追踪，失败可重发。  
**FR-055** 每日汇总邮件需包含当天新增已发布内容列表、摘要与详情页链接。  

### 8.7 后台管理
**FR-060** 仪表盘展示：当日新增内容数、已发布数、失败数、订阅新增、工作流成功率。  
**FR-061** 订阅者列表页：支持查看邮箱、状态、订阅时间、退订状态。  
**FR-062** 内容列表页：支持查看原文、译文、状态、来源、内容类型、发布时间。  
**FR-063** 内容详情页：支持查看原文 / 译文 / 渲染预览 / 错误日志 / 运行记录。  
**FR-064** 每日工作流执行页：展示每天的 discover / fetch / extract / translate / publish / notify 执行情况。  
**FR-065** 支持从指定阶段重试失败任务。  
**FR-066** 支持手动发布到 Web。  
**FR-067** 支持手动录入其他文章链接并自动抓取。  
**FR-068** 支持纯手工录入标题、摘要、正文、来源信息。  

### 8.8 微信公众号手动发布页
**FR-070** 后台提供“发布到微信公众号草稿”页面，不参与自动发布主流程。  
**FR-071** 支持选择站内已发布内容生成公众号草稿，并默认复用封面、摘要和作者信息。  
**FR-072** 支持直接手动录入标题、作者、正文，生成公众号草稿。  
**FR-073** 记录 `wechat_draft_id`、发布时间、错误码与响应摘要。  
**FR-074** 失败后支持管理员重试。  

### 8.9 文末署名与声明
**FR-080** 文末自动追加固定区块（模板可配置），至少包含：来源站点名称、原文标题、作者 / 讲者、发布时间、原文链接、免责声明。  

---

## 9. 权限、合规与风控（产品约束）

### 9.1 内容版权与转载风险
系统提供“署名 / 原文链接 / 来源声明”能力，但不保证获得转载授权。产品需显式提示运营者注意来源站点条款，并保留“仅摘要 + 原文链接”的降级策略。

### 9.2 邮件合规
订阅邮件应包含退订入口，并记录用户订阅与退订状态，避免滥发。

### 9.3 平台风控
公众号能力改为后台手动触发，以降低误发与平台接口异常对主产品的影响。

---

## 10. 数据模型（概念）

### 10.1 ContentItem（内容）
- id
- source
- content_type（article / video / manual）
- url / canonical_url / source_item_id
- title / author_or_speaker / published_at
- raw_html / raw_text / transcript_text
- content_ast / content_md
- zh_ast / zh_md
- research_report_md
- web_slug / web_status / published_at_web
- dedupe_key
- metrics: timings（各阶段耗时）
- error: last_error_code / last_error_message / last_error_stage

### 10.2 Subscriber（订阅者）
- id
- email
- status（active / unsubscribed / bounced）
- subscribed_at
- unsubscribed_at
- source_page
- last_sent_at

### 10.3 PublishRecord（发布记录）
- id
- content_item_id
- channel（web / wechat_draft / email）
- status
- external_id
- triggered_by
- created_at / updated_at
- error_summary

### 10.4 RunLog（运行记录）
- run_id
- content_item_id
- stage
- started_at / ended_at
- outcome
- error_summary

---

## 11. 技术方案（与 PRD 相关的约束 / 接口）

> PRD 不锁死实现，但给出可行边界，便于估算与验收。

- 应用形态：Web 站点 + 后台管理 + 定时工作流。
- 编排：LangGraph 或等价的状态机 / DAG 编排方案，支持 checkpoint 与节点重试。
- 存储：SQLite（MVP）-> Postgres（增强 / 多用户）。
- 抽取：Readability / Trafilatura 类正文抽取；视频优先使用公开字幕；无字幕时需支持音频提取与自动转录。
- 翻译：可配置 LLM 提供方；需支持术语表与代码块保护。
- 研究报告：基于视频转录文字稿生成结构化深度研究报告，用于后台审核和站内扩展展示。
- Web：服务端渲染或前后端分离均可，但必须交付公开站点和后台管理界面。
- 邮件：使用可追踪的邮件服务供应商，支持订阅、退订、每日汇总邮件与投递结果回执。
- 微信接口：仅用于后台手动创建草稿，需独立封装，避免耦合主站发布链路。

---

## 12. 埋点与报表（MVP）

### 12.1 事件与指标
- discover_new_count（每次巡检新增内容数）
- pipeline_success_rate（端到端处理成功率）
- stage_latency_ms（各阶段耗时分布）
- publish_web_count（站内发布数量）
- subscriber_growth（日 / 周新增订阅）
- email_digest_send_success_rate（每日汇总邮件发送成功率）
- manual_publish_count（后台手动发布次数）

### 12.2 告警（P1）
- 连续 N 次巡检失败
- 连续 N 条内容在同一阶段失败
- 邮件发送连续失败
- 微信公众号草稿接口异常率过高

---

## 13. 里程碑与交付拆分（建议）

### Milestone 0：可上线浏览（1-2 周）
- 完成两来源内容发现、抓取、抽取、翻译、Web 发布链路。
- 完成公开站点首页 / 列表 / 详情页。

### Milestone 1：可运营（2-3 周）
- 完成后台仪表盘、内容管理、订阅者管理、每日工作流执行页。
- 完成邮箱订阅与基础邮件分发。

### Milestone 2：渠道扩展（1-2 周）
- 完成后台手动录入其他文章。
- 完成后台手动创建微信公众号草稿。

---

## 14. 验收标准（MVP）

1) 对 OpenAI 与 Anthropic 各选 5 条历史公开内容进行回放处理，其中至少包含文章与视频资料：  
   - 10/10 成功生成可审核中文内容；  
   - 标题层级、代码块、主要链接不丢失；  
   - 视频内容若有字幕，字幕翻译结果可用。  
2) 新内容发现：在模拟新增或实际新增情况下，系统能在调度周期内发现并入库。  
3) 去重：同一内容重复巡检不会重复创建站内内容记录。  
4) 后台可追溯：任一失败能看到失败阶段与错误摘要，并支持从失败点重试。  
5) 公开站点可访问：首页、列表页、详情页可正常浏览。  
6) 邮件订阅链路可用：用户可完成订阅和退订，发布后能收到邮件通知。  
7) 后台管理可用：可查看订阅者列表、文章及译文列表、每日工作流执行情况。  
8) 后台手动发布可用：管理员可以手动发布其他文章，并可以在后台创建微信公众号草稿。  

---

## 15. 风险与对策

- **来源结构变化** -> 抽取失败：保留原始内容、增加规则修正与失败告警。  
- **视频无字幕或字幕质量差**：通过音频提取 + 自动转录 + 深度研究报告兜底，但需关注转录质量与成本。  
- **翻译质量波动**：使用术语表、结构校验与人工审核兜底。  
- **邮件触达不稳定**：选用支持投递结果回执的服务商，并提供重发机制。  
- **公众号接口限制 / 变更**：将该能力限定为后台独立工具，不影响主站核心流程。  
- **版权 / 合规不确定**：保留来源声明、原文链接和摘要模式。  

---

## 16. 已确认产品决策

1) 视频来源范围包括更多公开视频平台，不局限于官网嵌入页或单一视频渠道。  
2) 视频无字幕时，后台必须提取音频、自动转录为文字稿，并基于文字稿输出深度研究报告。  
3) 订阅邮件采用每日汇总策略作为 MVP 默认方案。  
4) 后台手动录入同时支持 URL 自动抓取与纯手工录入两种模式。  
5) 微信公众号手动发布页默认复用站内内容封面、摘要和作者信息。  

---

## 17. 附录

### 17.1 文末模板（示例）
> 本文为公开英文内容的中文整理 / 翻译版本。  
> 来源：{source_name}  
> 原文标题：{original_title}  
> 作者 / 讲者：{author_or_unknown}  
> 发布时间：{published_at_or_unknown}  
> 原文链接：{canonical_url}  
> 如有版权问题请联系处理。  

### 17.2 术语表示例（glossary.yml）
```yaml
OpenAI: OpenAI
Anthropic: Anthropic
token: Token
function calling: 函数调用
rate limit: 速率限制
model context protocol: 模型上下文协议
```
